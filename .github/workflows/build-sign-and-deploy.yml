name: Build, Sign, and Deploy

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]

env:
  DOCKER_REGISTRY: docker.io
  DOCKER_IMAGE_NAME: teq0v2/musical-umbrella

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      image-digest: ${{ steps.build.outputs.digest }}
      image-tag: ${{ steps.meta.outputs.tags }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Read VERSION file
      id: version
      run: |
        if [ -f VERSION ]; then
          version=$(cat VERSION | tr -d '\n\r' | tr -d '[:space:]')
          if [ -z "$version" ]; then
            version="0.0.0"
          fi
        else
          version="0.0.0"
        fi
        echo "version=$version" >> $GITHUB_OUTPUT

    - name: Get short SHA
      id: sha
      run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.19'

    - name: Run tests
      run: go test -v ./...

    - name: Build application
      run: go build -v .

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Install Cosign
      uses: sigstore/cosign-installer@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: ${{ env.DOCKER_REGISTRY }}
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}
        tags: |
          type=raw,value=${{ steps.version.outputs.version }}-${{ steps.sha.outputs.sha }},enable={{is_default_branch}}
          type=ref,event=pr,prefix=pr-${{ steps.version.outputs.version }}-${{ steps.sha.outputs.sha }}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      id: build
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: |
          ${{ steps.meta.outputs.labels }}
          org.opencontainers.image.title=${{ github.repository }}
          org.opencontainers.image.description=Musical Umbrella Go Application
          org.opencontainers.image.vendor=${{ github.repository_owner }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        provenance: false
        sbom: false

    - name: Get first image tag for scanning
      id: scan-image
      run: |
        first_tag=$(echo "${{ steps.meta.outputs.tags }}" | head -n1)
        echo "ref=${first_tag}@${{ steps.build.outputs.digest }}" >> $GITHUB_OUTPUT

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ steps.scan-image.outputs.ref }}
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-results.sarif'

    - name: Sign container image
      env:
        COSIGN_EXPERIMENTAL: 1
      run: |
        images=""
        for tag in $(echo "${{ steps.meta.outputs.tags }}" | tr '\n' ' '); do
          images+="${tag}@${{ steps.build.outputs.digest }} "
        done
        cosign sign --yes ${images}

    - name: Generate SLSA provenance with scan results
      uses: actions/attest-build-provenance@v1
      with:
        subject-name: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}
        subject-digest: ${{ steps.build.outputs.digest }}
        push-to-registry: true

    - name: Attest vulnerability scan results
      uses: actions/attest@v1
      with:
        subject-name: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}
        subject-digest: ${{ steps.build.outputs.digest }}
        predicate-type: 'https://github.com/in-toto/attestation/tree/main/spec/predicates/vuln'
        predicate: |
          {
            "invocation": {
              "uri": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
              "event_id": "${{ github.run_id }}",
              "builder_id": "trivy"
            },
            "scanner": {
              "name": "trivy",
              "version": "latest"
            },
            "metadata": {
              "scanStartedOn": "${{ steps.build.outputs.digest }}",
              "scanFinishedOn": "${{ github.run_id }}"
            }
          }

    - name: Create build summary
      run: |
        echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
        echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
        echo "| Image Digest | \`${{ steps.build.outputs.digest }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Git SHA | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Git Ref | \`${{ github.ref }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Build Date | \`$(date -u)\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Actor | \`${{ github.actor }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Security Scan | ✅ Completed |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Image Tags" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY